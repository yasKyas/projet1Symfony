{% extends 'base.html.twig' %}

{% block title %}Créer un Nouveau Contrat{% endblock %}

{% block body %}
    <h1>Créer nouveau Contrat</h1>
    {% if nbjours==0 %}
        <div class="alert alert-danger">{{ error}}</div>

    {% endif %}

    {{ include('contrat/_form.html.twig') }}

    <a href="{{ path('contrat_index') }}">retour</a>
{% endblock %}
