{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.Nom }}</td>
            </tr>
            <tr>
                <th>Numero de permis</th>
                <td>{{ client.Numerodepermis }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ client.ville }}</td>
            </tr>
            <tr>
                <th>Tek</th>
                <td>{{ client.tek }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_index') }}">retour</a>

    <a href="{{ path('client_edit', {'id': client.id}) }}" class="btn btn-danger btn-sm">Modifier</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
