{% extends 'base.html.twig' %}

{% block title %}Hello Voiture{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<h1>Voitures</h1>
<ul>
{% for voiture in voitures %}
<li>{{ voiture.marque|e }} {{ voiture.couleur|e }} {{ voiture.description|e }} {{
voiture.Nbrplace|e }} Places {{ voiture.idagence|e }} 
{% if voiture.disponibilite %}
        

<a href="{{ path('contrat_nouveau') }}" class="btn btn-danger btn-sm" target="_blank">louer</a> 


{% else%}
<a href="{{ path('app_rendre', {mat: voiture.matricule}) }}" class="btn btn-danger btn-sm" target="_blank">rendre</a> 				

       					 {% endif%} 
                            </li>
{% endfor %}
</ul>

{% endblock %}
